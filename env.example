# SearXNG is run separately with Docker Compose on host port 6667.
# JSON output must be enabled on any externally managed instance.
SEARXNG_URL=http://127.0.0.1:6667
# ENV_FILE=/path/to/another/.env

# API server
HOST=0.0.0.0
PORT=6666

# Retrieval limits
HTTP_CONCURRENCY=10
BROWSER_CONCURRENCY=3
PAGE_TIMEOUT_SECONDS=15
SEARCH_TIMEOUT_SECONDS=10
BROWSER_TIMEOUT_MS=20000
BROWSER_SETTLE_MS=750
MAX_PAGE_BYTES=5242880
MIN_TEXT_LENGTH=300

# Safety: by default, retrieved search-result URLs may not resolve to
# localhost/private/link-local addresses.
ALLOW_PRIVATE_URLS=false
